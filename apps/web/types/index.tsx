export type Child = {
    children: React.ReactNode;
};

export type Form = {
    id: string;
    name: string;
    email: string;
    mobile: string;
    bio: string;
    dob: Date;
    pronoun: string;
    partnerPronoun: string;
    passYear: number;
    department: string;
};
